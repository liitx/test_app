import 'package:flutter/material.dart';

buildInitialInput() {}

buildLoading() {
  return const Center(
    child: CircularProgressIndicator(),
  );
}

buildLoaded(
  post,
) {}

buildError(state) {}
